{{#section 'head'}}
<title>JB@ch - {{ title }}</title>
{{/section}}

<div>
  <h1>{{ title }}</h1>
  <div>
    {{#each posts}}
    <div class="mt-4 first:mt-0">
      {{> post id=id content=content createdAt=createdAt}}
    </div>
    {{/each}}
  </div>
</div>
